# Phase B Candidate Scope

This branch implements only the Portfolio-authorized one-shot synthetic benchmark. It does not deploy, purchase, contact the vendor, install a Decloak integration, or touch a production/customer resource.

Deployment remains a separate step after the implementation candidate passes independent review and validation.
