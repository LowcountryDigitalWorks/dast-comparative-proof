# Decloak One-Shot Vendor Handoff Template

Status: **DO NOT SEND UNTIL DEPLOYED TARGET FREEZE PASSES**

Provide the vendor only:

- the exact disposable workers.dev target URL;
- explicit confirmation that Lowcountry Digital Works owns the target and authorizes active testing of that exact hostname;
- request for one Enterprise scan sequence with full investigation, Active Testing/DAST, API discovery/testing, and AI Pentesting enabled;
- synthetic session-entry URL only if authenticated mode can be attached to that same scan from Decloak's own test environment.

Do not provide the repository URL, source, route matrix, expected vulnerabilities, seeded-condition IDs, customer data, production credentials/sessions, or any private source.

If authenticated mode cannot be attached to the same scan, mark D14 NOT TESTED and do not request a second courtesy scan.
